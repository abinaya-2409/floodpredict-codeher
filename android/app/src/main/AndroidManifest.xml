<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.floodypredict.app">

    <!-- ============================================================
         Bluetooth Permissions – Android API 31+ (Android 12 / S)
         ============================================================ -->
    <!-- Scan for nearby BLE devices (neverForLocation flag prevents
         location-data access requirement on Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- Advertise BLE service UUID so nearby devices discover us -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Connect to GATT server or RFCOMM SPP socket on peer device -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- ============================================================
         Bluetooth Permissions – Android API ≤ 30 (Android 11 / R)
         These are required as a fallback for older devices.
         ============================================================ -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- Fine location required for BLE scan on Android 6–11 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- ============================================================
         Internet (used by Floodylink for telemetry/API – NOT used
         for Bluetooth message delivery)
         ============================================================ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Declare BLE hardware as optional so app installs on non-BLE devices
         (non-BLE users still have full Floodylink flood intelligence) -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBarLaunch">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
